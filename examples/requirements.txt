# Python dependencies for Kronforce examples
#
# Install: pip install -r examples/requirements.txt
#
# Note: The MCP SDK requires Python 3.10+
# If you're on an older Python, install only requests:
#   pip install requests

# custom_agent.py — pull-based custom agent
requests>=2.28

# mcp_test_server.py — MCP test server (requires Python 3.10+)
mcp>=1.0; python_version >= "3.10"
