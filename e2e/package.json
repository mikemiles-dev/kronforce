{
  "name": "kronforce-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end Playwright tests for Kronforce. Run with `npm test`.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
